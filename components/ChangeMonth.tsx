import { dateMonthYear } from "types/graphql/graphql"

export default function ChangeMonth({month}: dateMonthYear): JSX.Element {
  return (
    <div className="show_month" style={{}}>
      <h1> yo yo yo</h1>
    </div>
  )
}